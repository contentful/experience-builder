import React from 'react';
import styles from './overlay.module.css';

// do something fancy here in the future
const LoaderOverlay = () => {
  return (
    <div className={styles.overlay}>
      <div className={styles.text}>...</div>
    </div>
  );
};

export default LoaderOverlay;
