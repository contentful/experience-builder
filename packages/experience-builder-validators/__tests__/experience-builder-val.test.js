'use strict';

const experienceBuilderVal = require('..');
const assert = require('assert').strict;

assert.strictEqual(experienceBuilderVal(), 'Hello from experienceBuilderVal');
console.info('experienceBuilderVal tests passed');
