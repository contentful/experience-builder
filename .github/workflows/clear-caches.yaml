name: Clear all Github actions caches
on:
  workflow_dispatch:

permissions:
  actions: write  # Delete cache

jobs:
  my-job:
    name: Delete all caches
    runs-on: ubuntu-latest

    steps:
      - name: Clear caches
        uses: easimon/wipe-cache@main